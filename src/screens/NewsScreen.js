import React from "react";
import { Text, StyleSheet } from "react-native";

const NewsScreen = () => {
  return <Text style={styles.text}>NewsScreen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default NewsScreen;
