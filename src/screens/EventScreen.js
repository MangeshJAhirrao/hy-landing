import React from "react";
import { Text, StyleSheet } from "react-native";

const EventScreen = () => {
  return <Text style={styles.text}>Event Screen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default EventScreen;
