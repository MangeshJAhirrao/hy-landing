import React from "react";
import { Text, StyleSheet } from "react-native";

const AboutUsScreen = () => {
  return <Text style={styles.text}>About Us</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default AboutUsScreen;
